# Kira Docker Configuration
# Copy to .env and adjust values before starting:
#   cp .env.example .env
#
# WARNING: Never commit .env to version control.

# ---- Required ----

# JWT secret for authentication tokens.
# Generate a strong random value:  openssl rand -hex 32
KIRA_JWT_SECRET=CHANGE_ME_BEFORE_FIRST_START

# ---- Authentication ----

# "mock" for development (no real auth), "centauth" for production SSO
KIRA_AUTH_MODE=mock

# CentAuth settings (only needed when KIRA_AUTH_MODE=centauth)
# KIRA_CENTAUTH_URL=https://centauth.example.com
# KIRA_CENTAUTH_APP_NAME=kira
# KIRA_CENTAUTH_VERIFY_SSL=true

# ---- Backend ----

# Paths inside the container (usually no need to change)
# KIRA_DB_PATH=/app/.kira/kanban.db
# KIRA_CHROMADB_PATH=/app/.kira/chromadb

# Allowed CORS origins (comma-separated)
# KIRA_CORS_ORIGINS=http://localhost,http://localhost:80

# Debug mode (do NOT enable in production)
# KIRA_DEBUG=false

# ---- Worker (host-side agent, not containerized) ----

# KIRA_SERVER_URL=http://localhost:8000
# KIRA_WORKSPACE_ROOT=~/.kira/workspaces
